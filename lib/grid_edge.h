// Licensed under the MIT License.

/** Represents a weighted edge in a grid-based graph. */
struct GridEdge
{
    int weight;
    int distance;
};

/** Represents a weighted edge in a grid-based graph. */
typedef struct GridEdge* GridEdge;
