#include "list.h"

/**
 * Stores all prime numbers in the interval [2, `max`) in a given list.
 *
 * @param list the list.
 * @param max  the exclusive upper bound of the prime sequence.
 * @return The exception; otherwise, `0`.
*/
Exception prime_list(List list, long max);
