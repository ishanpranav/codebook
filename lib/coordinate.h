// Licensed under the MIT License.

#include <stddef.h>

/** Represents a coordinate in two-dimensional space. */
struct Coordinate
{
    size_t i;
    size_t j;
};

/** Represents a coordinate in two-dimensional space. */
typedef struct Coordinate* Coordinate;
